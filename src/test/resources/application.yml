spring:
  config:
    activate:
      on-profile: test

  profiles:
    active: test

  datasource:
    url: jdbc:h2:mem:test;MODE=MYSQL
    username: test
    password: test

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

authentication:
  jwt:
    expiration: 1h
    secret: KL4xHZeqaesSNpfonXEnTd3FMG3fUQYyXkYUvYCyuoE=
